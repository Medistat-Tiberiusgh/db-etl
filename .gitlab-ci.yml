stages:
  - diagnostic

check_connectivity:
  stage: diagnostic
  image: alpine:latest
  allow_failure: true
  script:
    - apk add --no-cache iputils
    - ping -c 4 $SERVER_HOST && echo "Server is reachable" || echo "Server is NOT reachable (ICMP may be blocked)"
